// Genady Kogan
import React, { ReactElement } from "react";
import LeftMenuContainer from "./LeftMenuContainer";

const LeftMenu: React.FC = (): ReactElement => {
  return <LeftMenuContainer></LeftMenuContainer>;
};

export default LeftMenu;
